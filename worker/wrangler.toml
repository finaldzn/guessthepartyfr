name = "dlp-stats"
main = "src/index.js"
compatibility_date = "2026-05-01"

# Replace database_id with the value printed by `wrangler d1 create dlp`.
[[d1_databases]]
binding       = "DB"
database_name = "dlp"
database_id   = "REPLACE_AFTER_d1_create"
